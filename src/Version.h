#pragma once

// PokeyForge version (semver MAJOR.MINOR.PATCH).
// Bump this when cutting a release and add a matching entry to CHANGELOG.md.
// Defined as a macro so it can be string-concatenated with literals
// (e.g. `"PokeyForge " POKEYFORGE_VERSION`).
#define POKEYFORGE_VERSION "1.1.0"
